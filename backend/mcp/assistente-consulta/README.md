# MCP — Consulta ampliada do Assistente

Tools: `saude_turma`, `resumo_provas_turma`, `buscar_blocos`, `resultados_bloco`, `resumo_jornadas_professor`, `boletim_aluno`, `faltas_aluno`.

## Setup

```bash
cd src/mcp/assistente-consulta && npm install
```

Env: `EDUCATUDO_BASE_URL`, `EDUCATUDO_COOKIE`, `EDUCATUDO_CSRF`.

Endpoint: `POST /admin/consulta-assistente/mcp/ferramenta`
